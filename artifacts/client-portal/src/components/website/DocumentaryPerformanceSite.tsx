import type { CSSProperties, ReactNode } from "react";
import type { SitePlanV1 } from "@workspace/site-plan";

type Copy = { headline: string; subheadline: string; about: string; button: string };
type Section = { id: string; eyebrow?: string; title: string; body: string; highlights?: string[] };
type Brief = { businessName: string; mainService: string; audience: string; location: string; deliveryMode?: string; offer: string; credentials: string; results: string; testimonialQuote: string; testimonialName: string; process: string; faqQuestion: string; faqAnswer: string; schedule?: string; serviceDetails?: string; sections?: string[] };
type Part = "headline" | "subheadline" | "about" | "button";
export type DocumentaryPerformanceProps = { compositionId: string; brief: Brief; copy: Copy; sections: Section[]; heroImage: string; useImage: boolean; accent: string; narrow: boolean; editable?: boolean; selectedPart?: string; onSelect?: (part: Part) => void; onReplaceImage?: () => void; sitePlan?: SitePlanV1; fontStyle?: string; paletteDark?: string; paletteLight?: string; previewMode?: "desktop" | "tablet" | "mobile" };

const cn = (...values: Array<string | false | null | undefined>) => values.filter(Boolean).join(" ");
const splitItems = (value = "") => value.split(/\n|;|\u2022/).map((item) => item.trim()).filter(Boolean).slice(0, 5);
const sectionOf = (sections: Section[], id: string) => sections.find((item) => item.id === id);
const cleanQuote = (value: string) => value.trim().replace(/^[“"']+|[”"']+$/g, "");
const escapeHtml = (value: string) => value.replace(/[&<>'"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#039;", '"': "&quot;" })[char] || char);

function resolveDocumentary(props: DocumentaryPerformanceProps) {
  const ids = ["services", "approach", "about", "results", "testimonial", "faq", "contact"];
  const byId = Object.fromEntries(ids.map((id) => [id, sectionOf(props.sections, id)])) as Record<string, Section | undefined>;
  const selected = new Set(props.brief.sections || []);
  const schedule = splitItems(props.brief.schedule);
  const available = [
    byId.services && selected.has("services") ? "services" : "", byId.approach && selected.has("approach") ? "approach" : "",
    schedule.length && selected.has("schedule") ? "schedule" : "", byId.about && selected.has("about") ? "about" : "",
    (byId.results || props.brief.credentials || props.brief.results) && selected.has("results") ? "results" : "",
    props.brief.testimonialQuote && selected.has("testimonial") ? "testimonial" : "",
    props.brief.faqQuestion && props.brief.faqAnswer && selected.has("faq") ? "faq" : "", selected.has("contact") ? "contact" : "",
  ].filter(Boolean);
  const ordered = [...new Set([...(props.sitePlan?.narrative.orderedModuleIds || []), ...available])].filter((id) => available.includes(id));
  const contactIndex = ordered.indexOf("contact"); if (contactIndex >= 0) ordered.push(...ordered.splice(contactIndex, 1));
  const images = props.useImage ? [...new Set([props.heroImage, ...(props.sitePlan?.assets?.images.map((asset) => asset.url) || [])].filter(Boolean))] : [];
  const requested = props.sitePlan?.hero.variant || (images.length > 2 ? "sequence" : "dossier");
  const heroVariant = ["dossier", "full-bleed", "sequence"].includes(requested) ? requested : "dossier";
  const fit = props.sitePlan?.responsive.headlineFit || (props.copy.headline.length > 72 ? "compact" : props.copy.headline.length > 42 ? "balanced" : "display");
  const displayFont = props.fontStyle === "Premium editorial" ? 'Georgia,"Times New Roman",serif' : props.fontStyle === "Clean professional" ? '"Arial Narrow",Arial,sans-serif' : '"Arial Black",Arial,sans-serif';
  const ink = props.paletteDark || "#0b0b0b", paper = props.paletteLight || "#f5f2ec", signal = props.accent || "#ef5a3c";
  const steps = byId.approach?.highlights?.length ? byId.approach.highlights : splitItems(props.brief.process);
  const services = byId.services?.highlights?.length ? byId.services.highlights : [props.brief.serviceDetails || byId.services?.body || props.brief.offer].filter(Boolean);
  const proof = [...new Set([byId.results?.body, props.brief.credentials, props.brief.results].filter((item): item is string => Boolean(item)))];
  return { byId, schedule, ordered, images, heroVariant, fit, displayFont, ink, paper, signal, steps, services, proof };
}

function Editable({ part, props, className, children }: { part: Part; props: DocumentaryPerformanceProps; className?: string; children: ReactNode }) {
  return <button disabled={!props.editable} onClick={() => props.onSelect?.(part)} className={cn("block text-left", className, props.editable && "outline-none hover:ring-2 hover:ring-white/50", props.editable && props.selectedPart === part && "ring-2 ring-white")}>{children}</button>;
}

export function DocumentaryPerformanceSite(props: DocumentaryPerformanceProps) {
  const r = resolveDocumentary(props); const { byId, schedule, ordered, images, heroVariant, fit, displayFont, ink, paper, signal, steps, services, proof } = r;
  const compact = props.previewMode === "tablet" || props.previewMode === "mobile"; const mobile = props.previewMode === "mobile" || props.narrow;
  const vars = { "--doc-ink": ink, "--doc-paper": paper, "--doc-signal": signal, "--doc-display": displayFont } as CSSProperties;
  const heroSize = mobile ? fit === "compact" ? "text-[3rem]" : "text-[4rem]" : fit === "compact" ? "text-[4.7rem]" : fit === "balanced" ? "text-[6rem]" : "text-[7.5rem]";
  const heading = cn("doc-display font-black uppercase leading-[.88] tracking-[-.065em]", mobile ? "text-[3rem]" : compact ? "text-[4rem]" : "text-[5.6rem]");
  const pad = mobile ? "px-6 py-16" : "px-12 py-24";
  const action = <Editable part="button" props={props} className="inline-flex items-center gap-8 px-5 py-4 text-[10px] font-black uppercase tracking-[.12em]" ><span>{props.copy.button}</span><span>↗</span></Editable>;
  const mainImage = <button disabled={!props.editable} onClick={props.onReplaceImage} className="relative min-h-[520px] overflow-hidden bg-[#242424] text-left">{images[0] && <img src={images[0]} alt="" className="absolute inset-0 h-full w-full object-cover grayscale" />}<div className="absolute inset-0 bg-gradient-to-t from-black via-black/10 to-transparent" /><span className="absolute left-6 top-6 border border-white/40 px-3 py-2 text-[9px] font-black uppercase tracking-[.18em]">Field image / 01</span></button>;
  const heroCopy = <div className={cn("relative z-10 flex min-h-[680px] flex-col justify-between", mobile ? "p-6 py-12" : "p-12")}><div className="flex justify-between text-[9px] font-black uppercase tracking-[.2em]"><span style={{ color: signal }}>{props.brief.mainService}</span><span>Coaching dossier / 01</span></div><div className="my-12"><Editable part="headline" props={props}><h1 className={cn("doc-display max-w-5xl font-black uppercase leading-[.84] tracking-[-.075em]", heroSize)}>{props.copy.headline}</h1></Editable><Editable part="subheadline" props={props} className="mt-8 max-w-xl text-lg leading-8 text-white/65">{props.copy.subheadline}</Editable><div className="mt-8 inline-block" style={{ background: signal, color: ink }}>{action}</div></div><div className="flex justify-between text-[9px] font-black uppercase tracking-[.18em] text-white/45"><span>{props.brief.audience}</span><span>{props.brief.location}</span></div></div>;
  const module = (id: string, index: number) => { const n = String(index + 1).padStart(2, "0");
    if (id === "services" && byId.services) return <section key={id} data-module="services" className={pad} style={{ background: paper, color: ink }}><div className="flex justify-between border-t-2 border-current pt-3 text-[9px] font-black uppercase tracking-[.18em]"><span>{byId.services.eyebrow || "Service routes"}</span><span>File {n}</span></div><div className={cn("mt-14 grid gap-12", compact ? "grid-cols-1" : "grid-cols-[.7fr_1.3fr]")}><h2 className={heading}>{byId.services.title}</h2><div><p className="text-lg leading-8">{byId.services.body}</p><div className="mt-8 divide-y-2 divide-black border-y-2 border-black">{services.slice(0, 3).map((item, i) => <div key={item} className="grid grid-cols-[48px_1fr] py-5"><span style={{ color: signal }}>0{i + 1}</span><b>{item}</b></div>)}</div></div></div></section>;
    if (id === "approach" && byId.approach) return <section key={id} data-module="approach" className={pad} style={{ background: ink, color: paper }}><div className="flex justify-between border-t border-white/30 pt-3 text-[9px] font-black uppercase tracking-[.18em]"><span>{byId.approach.eyebrow || "The method"}</span><span>Sequence {n}</span></div><div className={cn("mt-14 grid gap-14", compact ? "grid-cols-1" : "grid-cols-[1fr_1fr]")}><div><h2 className={heading}>{byId.approach.title}</h2><p className="mt-7 max-w-xl text-base leading-7 text-white/65">{byId.approach.body}</p></div><ol className="divide-y divide-white/20 border-y border-white/20">{steps.map((item, i) => <li key={item} className="grid grid-cols-[52px_1fr] py-6"><span style={{ color: signal }}>0{i + 1}</span><b className="text-xl">{item}</b></li>)}</ol></div></section>;
    if (id === "schedule") return <section key={id} data-module="schedule" className={pad} style={{ background: paper, color: ink }}><div className="text-[9px] font-black uppercase tracking-[.18em]" style={{ color: signal }}>Recorded schedule / {n}</div><h2 className={cn(heading, "mt-5")}>Training on the record.</h2><div className="mt-10 divide-y-2 divide-black border-y-2 border-black">{schedule.map((item, i) => <div key={item} className="grid grid-cols-[52px_1fr] py-5"><span>0{i + 1}</span><b>{item}</b></div>)}</div></section>;
    if (id === "about" && byId.about) return <section key={id} data-module="about" className={cn("grid", compact ? "grid-cols-1" : "grid-cols-[.8fr_1.2fr]")} style={{ background: ink, color: paper }}>{images[1] ? <img src={images[1]} alt="" className="h-full min-h-[480px] w-full object-cover grayscale" /> : <div className="grid min-h-[420px] place-items-center text-[10rem] font-black" style={{ background: signal, color: ink }}>{(props.brief.businessName || "C").charAt(0)}</div>}<div className={pad}><span className="text-[9px] font-black uppercase tracking-[.18em]" style={{ color: signal }}>{byId.about.eyebrow || "Coach notes"} / {n}</span><h2 className={cn(heading, "mt-7")}>{byId.about.title}</h2><Editable part="about" props={props} className="mt-8 max-w-2xl text-xl leading-8 text-white/70">{props.copy.about || byId.about.body}</Editable></div></section>;
    if (id === "results" && proof.length) return <section key={id} data-module="results" className={pad} style={{ background: paper, color: ink }}><div className="flex justify-between border-t-2 border-current pt-3 text-[9px] font-black uppercase tracking-[.18em]"><span>{byId.results?.eyebrow || "Verified in the brief"}</span><span>Evidence {n}</span></div><h2 className={cn(heading, "mt-12 max-w-5xl")}>{byId.results?.title || "The standard behind the work."}</h2><div className={cn("mt-12 grid border-2 border-black", compact ? "grid-cols-1" : "grid-cols-2")}>{proof.slice(0, 3).map((item, i) => <article key={item} className="min-h-48 border-black p-6 md:border-r-2"><span className="text-xs" style={{ color: signal }}>0{i + 1}</span><p className="mt-12 text-lg font-bold leading-7">{item}</p></article>)}</div></section>;
    if (id === "testimonial" && props.brief.testimonialQuote) return <section key={id} data-module="testimonial" className={pad} style={{ background: signal, color: ink }}><span className="text-[9px] font-black uppercase tracking-[.18em]">Client record / {n}</span><blockquote className={cn("doc-display mt-10 max-w-6xl font-black uppercase leading-[.94] tracking-[-.055em]", mobile ? "text-[2.5rem]" : "text-[4.5rem]")}>“{cleanQuote(props.brief.testimonialQuote)}”</blockquote>{props.brief.testimonialName && <cite className="mt-7 block text-[10px] not-italic uppercase tracking-[.16em]">{props.brief.testimonialName}</cite>}</section>;
    if (id === "faq") return <section key={id} data-module="faq" className={pad} style={{ background: paper, color: ink }}><span className="text-[9px] font-black uppercase tracking-[.18em]" style={{ color: signal }}>Before contact / {n}</span><div className={cn("mt-8 grid gap-12", compact ? "grid-cols-1" : "grid-cols-2")}><h2 className={heading}>{byId.faq?.title || "A direct answer."}</h2><details open className="border-y-2 border-black py-6"><summary className="flex cursor-pointer list-none justify-between gap-4 text-lg font-black"><span>{props.brief.faqQuestion}</span><span>+</span></summary><p className="mt-5 text-sm leading-7">{props.brief.faqAnswer}</p></details></div></section>;
    if (id === "contact") return <section key={id} data-module="contact" className={cn(pad, "min-h-[520px]")} style={{ background: ink, color: paper }}><span className="text-[9px] font-black uppercase tracking-[.18em]" style={{ color: signal }}>Next action / {n}</span><h2 className={cn(heading, "mt-8 max-w-5xl")}>{byId.contact?.title || "Put the work in motion."}</h2><p className="mt-7 max-w-xl text-lg leading-8 text-white/65">{byId.contact?.body || props.brief.offer || props.copy.subheadline}</p><div className="mt-8 inline-block" style={{ background: signal, color: ink }}>{action}</div></section>;
    return null; };
  const hero = heroVariant === "full-bleed" ? <section data-hero="full-bleed" className="relative min-h-[720px] overflow-hidden bg-black text-white">{images[0] && <img src={images[0]} alt="" className="absolute inset-0 h-full w-full object-cover grayscale" />}<div className="absolute inset-0 bg-gradient-to-r from-black via-black/65 to-black/10" />{heroCopy}</section> : heroVariant === "sequence" && !mobile ? <section data-hero="sequence" className="grid grid-cols-[1.1fr_.9fr] bg-black text-white"><div>{heroCopy}</div><div className="grid grid-rows-[1.35fr_.65fr]">{mainImage}<div className="grid grid-cols-2">{images.slice(1, 3).map((url, i) => <img key={url} src={url} alt="" className="h-full min-h-48 w-full object-cover grayscale" />)}{images.length < 2 && <div className="bg-white/10" />}</div></div></section> : <section data-hero="dossier" className={cn("grid bg-black text-white", compact ? "grid-cols-1" : "grid-cols-[1.2fr_.8fr]")}><div>{mainImage}</div><div>{heroCopy}</div></section>;
  return <div className="doc-preview overflow-hidden" style={{ ...vars, background: ink, color: paper, fontFamily: "Arial,Helvetica,sans-serif" }}><style>{`.doc-preview .doc-display{font-family:var(--doc-display)}.doc-preview button{font-family:inherit}`}</style><header className="flex items-center justify-between border-b border-white/20 px-6 py-5 md:px-12"><b className="text-sm uppercase tracking-[.18em]">{props.brief.businessName}</b><span className="text-[9px] uppercase tracking-[.2em] text-white/45">Documentary performance</span></header>{hero}{ordered.map(module)}<footer className="flex flex-wrap justify-between gap-5 border-t border-white/20 px-6 py-9 text-[9px] uppercase tracking-[.16em]"><b>{props.brief.businessName}</b><span>{props.brief.location}</span><span>{props.copy.button}</span></footer></div>;
}

export function buildDocumentaryPerformanceHtml(props: DocumentaryPerformanceProps, bookingUrl: string, locale: string) {
  const h = escapeHtml; const r = resolveDocumentary(props); const { byId, schedule, ordered, images, heroVariant, fit, displayFont, ink, paper, signal, steps, services, proof } = r;
  const action = `<a class="action" href="${h(bookingUrl)}">${h(props.copy.button)} <span>↗</span></a>`; const heroSize = fit === "compact" ? "clamp(3.8rem,7vw,7rem)" : fit === "balanced" ? "clamp(4.5rem,8vw,8.5rem)" : "clamp(5rem,9vw,9.5rem)";
  const label = (left: string, right: string) => `<div class="label"><span>${h(left)}</span><span>${h(right)}</span></div>`;
  const image = (url: string, className = "") => url ? `<img class="${className}" src="${h(url)}" alt="" width="1600" height="1200">` : `<div class="image-empty"></div>`;
  const heroCopy = `<div class="hero-copy"><div class="hero-top"><span>${h(props.brief.mainService)}</span><span>COACHING DOSSIER / 01</span></div><div><h1>${h(props.copy.headline)}</h1><p>${h(props.copy.subheadline)}</p>${action}</div><div class="hero-meta"><span>${h(props.brief.audience)}</span><span>${h(props.brief.location)}</span></div></div>`;
  const hero = heroVariant === "full-bleed" ? `<section class="hero full-bleed" data-hero="full-bleed">${image(images[0])}<div class="shade"></div>${heroCopy}</section>` : heroVariant === "sequence" ? `<section class="hero sequence" data-hero="sequence"><div>${heroCopy}</div><div class="sequence-images">${image(images[0])}<div>${images.slice(1, 3).map((url) => image(url)).join("")}</div></div></section>` : `<section class="hero dossier" data-hero="dossier"><figure>${image(images[0])}<span>FIELD IMAGE / 01</span></figure>${heroCopy}</section>`;
  const moduleHtml = (id: string, index: number) => { const n = String(index + 1).padStart(2, "0");
    if (id === "services" && byId.services) return `<section class="section services" id="services" data-module="services">${label(byId.services.eyebrow || "Service routes", `FILE ${n}`)}<div class="split"><h2>${h(byId.services.title)}</h2><div><p>${h(byId.services.body)}</p><div class="rows">${services.slice(0, 3).map((item, i) => `<div><span>0${i + 1}</span><b>${h(item)}</b></div>`).join("")}</div></div></div></section>`;
    if (id === "approach" && byId.approach) return `<section class="section method" id="approach" data-module="approach">${label(byId.approach.eyebrow || "The method", `SEQUENCE ${n}`)}<div class="split"><div><h2>${h(byId.approach.title)}</h2><p>${h(byId.approach.body)}</p></div><div class="rows">${steps.map((item, i) => `<div><span>0${i + 1}</span><b>${h(item)}</b></div>`).join("")}</div></div></section>`;
    if (id === "schedule") return `<section class="section schedule" data-module="schedule">${label("Recorded schedule", n)}<h2>Training on the record.</h2><div class="rows">${schedule.map((item, i) => `<div><span>0${i + 1}</span><b>${h(item)}</b></div>`).join("")}</div></section>`;
    if (id === "about" && byId.about) return `<section class="about" id="about" data-module="about">${images[1] ? image(images[1]) : `<div class="monogram">${h((props.brief.businessName || "C").charAt(0))}</div>`}<div class="section"><small>${h(byId.about.eyebrow || "COACH NOTES")} / ${n}</small><h2>${h(byId.about.title)}</h2><p>${h(props.copy.about || byId.about.body)}</p></div></section>`;
    if (id === "results" && proof.length) return `<section class="section evidence" id="results" data-module="results">${label(byId.results?.eyebrow || "Verified in the brief", `EVIDENCE ${n}`)}<h2>${h(byId.results?.title || "The standard behind the work.")}</h2><div class="proof">${proof.slice(0, 3).map((item, i) => `<article><span>0${i + 1}</span><p>${h(item)}</p></article>`).join("")}</div></section>`;
    if (id === "testimonial" && props.brief.testimonialQuote) return `<section class="section quote" data-module="testimonial"><small>CLIENT RECORD / ${n}</small><blockquote>“${h(cleanQuote(props.brief.testimonialQuote))}”</blockquote>${props.brief.testimonialName ? `<cite>${h(props.brief.testimonialName)}</cite>` : ""}</section>`;
    if (id === "faq") return `<section class="section faq" data-module="faq"><small>BEFORE CONTACT / ${n}</small><div class="split"><h2>${h(byId.faq?.title || "A direct answer.")}</h2><details open><summary>${h(props.brief.faqQuestion)}<span>+</span></summary><p>${h(props.brief.faqAnswer)}</p></details></div></section>`;
    if (id === "contact") return `<section class="section contact" id="contact" data-module="contact"><small>NEXT ACTION / ${n}</small><h2>${h(byId.contact?.title || "Put the work in motion.")}</h2><p>${h(byId.contact?.body || props.brief.offer || props.copy.subheadline)}</p>${action}</section>`; return ""; };
  const schema = JSON.stringify({ "@context": "https://schema.org", "@type": "ProfessionalService", name: props.brief.businessName, description: props.copy.subheadline, areaServed: props.brief.location || undefined, url: bookingUrl.startsWith("http") ? bookingUrl : undefined }).replace(/</g, "\\u003c");
  return `<!doctype html><html lang="${h(locale)}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${h(props.brief.businessName)} | ${h(props.brief.mainService)}</title><meta name="description" content="${h(props.copy.subheadline)}"><meta name="theme-color" content="${h(ink)}"><script type="application/ld+json">${schema}</script><style>
:root{--ink:${ink};--paper:${paper};--signal:${signal};--display:${displayFont};--gutter:clamp(1.5rem,5vw,4.5rem)}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--ink);color:var(--paper);font-family:Arial,Helvetica,sans-serif;overflow-x:hidden}a{color:inherit}a:focus-visible,summary:focus-visible{outline:3px solid var(--signal);outline-offset:4px}h1,h2,h3,p{margin-top:0}h1,h2,h3,blockquote{font-family:var(--display);font-weight:900;text-transform:uppercase}header,footer{display:flex;flex-wrap:wrap;justify-content:space-between;gap:1rem;padding:1.4rem var(--gutter);border-bottom:1px solid #ffffff33;font-size:.62rem;font-weight:900;letter-spacing:.17em;text-transform:uppercase}.hero{position:relative;min-height:43rem;background:#111;color:var(--paper)}.hero.dossier{display:grid;grid-template-columns:1.2fr .8fr}.hero.dossier figure{position:relative;min-height:43rem;margin:0;overflow:hidden}.hero figure img,.full-bleed>img,.sequence-images>img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;filter:grayscale(1)}.hero figure span{position:absolute;top:1.5rem;left:1.5rem;padding:.6rem;border:1px solid #ffffff66;font-size:.58rem;font-weight:900;letter-spacing:.16em}.hero-copy{position:relative;z-index:2;display:flex;min-height:43rem;flex-direction:column;justify-content:space-between;padding:3rem var(--gutter)}.hero-top,.hero-meta,.label{display:flex;justify-content:space-between;gap:1rem;font-size:.58rem;font-weight:900;letter-spacing:.17em;text-transform:uppercase}.hero-top span:first-child{color:var(--signal)}h1{margin:2.5rem 0 1.5rem;font-size:${heroSize};line-height:.84;letter-spacing:-.075em}.hero-copy p{max-width:38rem;font-size:1.1rem;line-height:1.7;color:#ffffffaa}.action{display:inline-flex;gap:2rem;margin-top:1.5rem;padding:1rem 1.3rem;background:var(--signal);color:var(--ink);font-size:.66rem;font-weight:900;letter-spacing:.12em;text-decoration:none;text-transform:uppercase}.full-bleed>img{z-index:0}.shade{position:absolute;inset:0;background:linear-gradient(90deg,#000e,#0008,#0001)}.full-bleed .hero-copy{max-width:75rem}.sequence{display:grid;grid-template-columns:1.1fr .9fr}.sequence-images{position:relative;display:grid;grid-template-rows:1.35fr .65fr;min-height:43rem}.sequence-images>div{display:grid;grid-template-columns:1fr 1fr}.sequence-images>div img{width:100%;height:100%;object-fit:cover;filter:grayscale(1)}.section{padding:6rem var(--gutter);background:var(--paper);color:var(--ink)}.label{padding-top:.75rem;border-top:2px solid}.split{display:grid;grid-template-columns:.75fr 1.25fr;gap:6vw;margin-top:3.5rem}.section h2,.about h2{margin:0;font-size:clamp(3.5rem,7vw,7rem);line-height:.88;letter-spacing:-.065em}.section p,.about p{font-size:1.05rem;line-height:1.8}.rows{margin-top:2rem;border-block:2px solid}.rows>div{display:grid;grid-template-columns:3.5rem 1fr;padding:1.25rem 0;border-bottom:1px solid}.rows>div:last-child{border:0}.rows span{color:var(--signal)}.method{background:var(--ink);color:var(--paper)}.method .rows{border-color:#ffffff55}.method .rows>div{border-color:#ffffff33}.schedule h2,.evidence h2{max-width:70rem;margin-top:3rem}.about{display:grid;grid-template-columns:.8fr 1.2fr;background:var(--ink);color:var(--paper)}.about>img,.monogram{width:100%;height:100%;min-height:32rem;object-fit:cover;filter:grayscale(1)}.monogram{display:grid;place-items:center;background:var(--signal);color:var(--ink);font:900 12rem var(--display)}.about .section{background:transparent;color:inherit}.about small,.quote small,.faq small,.contact small{font-size:.58rem;font-weight:900;letter-spacing:.17em}.about .section>p{margin-top:2rem;color:#ffffffaa}.proof{display:grid;grid-template-columns:repeat(2,1fr);margin-top:3rem;border:2px solid}.proof article{min-height:12rem;padding:1.5rem;border-right:2px solid}.proof article span{color:var(--signal)}.proof article p{margin-top:3rem;font-weight:700}.quote{background:var(--signal)}blockquote{max-width:75rem;margin:2rem 0 0;font-size:clamp(2.7rem,6vw,5.5rem);line-height:.94;letter-spacing:-.055em}.quote cite{display:block;margin-top:1.5rem;font-size:.65rem;font-style:normal;letter-spacing:.15em;text-transform:uppercase}.faq details{padding:1.5rem 0;border-block:2px solid}.faq summary{display:flex;justify-content:space-between;gap:1rem;font-weight:900;cursor:pointer;list-style:none}.faq details p{margin-top:1.25rem}.contact{min-height:32rem;background:var(--ink);color:var(--paper)}.contact h2{max-width:70rem;margin-top:2rem}.contact>p{max-width:40rem;color:#ffffffaa}footer{border-top:1px solid #ffffff33;border-bottom:0}
@media(max-width:760px){.hero.dossier,.hero.sequence,.split,.about{grid-template-columns:1fr}.hero.dossier figure{min-height:26rem}.hero-copy{min-height:38rem;padding:2.5rem 1.5rem}h1{font-size:${fit === "compact" ? "3rem" : "4rem"}}.sequence-images{min-height:28rem}.section{padding:4rem 1.5rem}.section h2,.about h2{font-size:3rem}.proof{grid-template-columns:1fr}.proof article{border-right:0;border-bottom:2px solid}.hero-meta{flex-direction:column}.full-bleed .shade{background:linear-gradient(0deg,#000e,#0007)}}
</style></head><body><header><b>${h(props.brief.businessName)}</b><span>Documentary performance</span></header><main>${hero}${ordered.map(moduleHtml).join("")}</main><footer><b>${h(props.brief.businessName)}</b><span>${h(props.brief.location)}</span><span>${h(props.copy.button)}</span></footer></body></html>`;
}
