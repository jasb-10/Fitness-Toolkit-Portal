import type { CSSProperties, ReactNode } from "react";
import type { SitePlanV1 } from "@workspace/site-plan";

type Copy = { headline: string; subheadline: string; about: string; button: string };
type Section = { id: string; eyebrow?: string; title: string; body: string; highlights?: string[] };
type Brief = { businessName: string; mainService: string; audience: string; location: string; deliveryMode?: string; offer: string; credentials: string; results: string; testimonialQuote: string; testimonialName: string; process: string; faqQuestion: string; faqAnswer: string; schedule?: string; serviceDetails?: string; sections?: string[] };
type Part = "headline" | "subheadline" | "about" | "button";
export type PrecisionPracticeProps = { compositionId: string; brief: Brief; copy: Copy; sections: Section[]; heroImage: string; useImage: boolean; accent: string; narrow: boolean; editable?: boolean; selectedPart?: string; onSelect?: (part: Part) => void; onReplaceImage?: () => void; sitePlan?: SitePlanV1; fontStyle?: string; paletteDark?: string; paletteLight?: string; previewMode?: "desktop" | "tablet" | "mobile" };

const cn = (...values: Array<string | false | null | undefined>) => values.filter(Boolean).join(" ");
const splitItems = (value = "") => value.split(/\n|;|\u2022/).map((item) => item.trim()).filter(Boolean).slice(0, 5);
const sectionOf = (sections: Section[], id: string) => sections.find((item) => item.id === id);
const cleanQuote = (value: string) => value.trim().replace(/^[“"']+|[”"']+$/g, "");
const escapeHtml = (value: string) => value.replace(/[&<>'"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#039;", '"': "&quot;" })[char] || char);

function resolvePrecision(props: PrecisionPracticeProps) {
  const ids = ["services", "approach", "about", "results", "testimonial", "faq", "contact"];
  const byId = Object.fromEntries(ids.map((id) => [id, sectionOf(props.sections, id)])) as Record<string, Section | undefined>;
  const selected = new Set(props.brief.sections || []);
  const schedule = splitItems(props.brief.schedule);
  const available = [
    byId.services && selected.has("services") ? "services" : "", byId.approach && selected.has("approach") ? "approach" : "",
    schedule.length && selected.has("schedule") ? "schedule" : "", byId.about && selected.has("about") ? "about" : "",
    (byId.results || props.brief.credentials || props.brief.results) && selected.has("results") ? "results" : "",
    props.brief.testimonialQuote && selected.has("testimonial") ? "testimonial" : "",
    props.brief.faqQuestion && props.brief.faqAnswer && selected.has("faq") ? "faq" : "", selected.has("contact") ? "contact" : "",
  ].filter(Boolean);
  const ordered = [...new Set([...(props.sitePlan?.narrative.orderedModuleIds || []), ...available])].filter((id) => available.includes(id));
  const contactIndex = ordered.indexOf("contact"); if (contactIndex >= 0) ordered.push(...ordered.splice(contactIndex, 1));
  const images = props.useImage ? [...new Set([props.heroImage, ...(props.sitePlan?.assets?.images.map((asset) => asset.url) || [])].filter(Boolean))] : [];
  const requested = props.sitePlan?.hero.variant || (images.length ? "practitioner" : "assessment");
  const heroVariant = ["assessment", "practitioner", "pathway"].includes(requested) ? requested : "assessment";
  const fit = props.sitePlan?.responsive.headlineFit || (props.copy.headline.length > 72 ? "compact" : props.copy.headline.length > 42 ? "balanced" : "display");
  const displayFont = props.fontStyle === "Premium editorial" ? '"Iowan Old Style",Baskerville,Georgia,serif' : props.fontStyle === "Strong & modern" ? '"Arial Black",Arial,sans-serif' : 'Inter,"Segoe UI",Arial,sans-serif';
  const ink = props.paletteDark || "#17312d", paper = props.paletteLight || "#f4f3ee", signal = props.accent || "#ef6b4a";
  const steps = byId.approach?.highlights?.length ? byId.approach.highlights : splitItems(props.brief.process);
  const services = byId.services?.highlights?.length ? byId.services.highlights : [props.brief.serviceDetails || byId.services?.body || props.brief.offer].filter(Boolean);
  const proof = [...new Set([byId.results?.body, props.brief.credentials, props.brief.results].filter((item): item is string => Boolean(item)))];
  return { byId, schedule, ordered, images, heroVariant, fit, displayFont, ink, paper, signal, steps, services, proof };
}

function Editable({ part, props, className, children }: { part: Part; props: PrecisionPracticeProps; className?: string; children: ReactNode }) {
  return <button disabled={!props.editable} onClick={() => props.onSelect?.(part)} className={cn("block text-left", className, props.editable && "outline-none hover:ring-2 hover:ring-current/30", props.editable && props.selectedPart === part && "ring-2 ring-current")}>{children}</button>;
}

export function PrecisionPracticeSite(props: PrecisionPracticeProps) {
  const r = resolvePrecision(props); const { byId, schedule, ordered, images, heroVariant, fit, displayFont, ink, paper, signal, steps, services, proof } = r;
  const compact = props.previewMode === "tablet" || props.previewMode === "mobile"; const mobile = props.previewMode === "mobile" || props.narrow;
  const vars = { "--precision-ink": ink, "--precision-paper": paper, "--precision-signal": signal, "--precision-display": displayFont } as CSSProperties;
  const titleSize = mobile ? fit === "compact" ? "text-[2.8rem]" : "text-[3.6rem]" : fit === "compact" ? "text-[4.1rem]" : fit === "balanced" ? "text-[5.1rem]" : "text-[6.3rem]";
  const heading = cn("precision-display font-medium leading-[.98] tracking-[-.055em]", mobile ? "text-[2.6rem]" : compact ? "text-[3.4rem]" : "text-[4.5rem]");
  const pad = mobile ? "px-6 py-16" : "px-12 py-24";
  const action = <Editable part="button" props={props} className="inline-flex items-center gap-8 border border-current px-5 py-4 text-[10px] font-bold uppercase tracking-[.12em]"><span>{props.copy.button}</span><span>→</span></Editable>;
  const heroCopy = <div className={cn("flex min-h-[670px] flex-col justify-between", mobile ? "p-6 py-10" : "p-12")}><div className="flex justify-between gap-5 border-t border-current pt-3 text-[9px] font-bold uppercase tracking-[.18em]"><span style={{ color: signal }}>{props.brief.mainService}</span><span>Assessment / 01</span></div><div className="my-14"><Editable part="headline" props={props}><h1 className={cn("precision-display max-w-5xl font-medium leading-[.9] tracking-[-.07em]", titleSize)}>{props.copy.headline}</h1></Editable><Editable part="subheadline" props={props} className="mt-8 max-w-xl text-lg leading-8 opacity-65">{props.copy.subheadline}</Editable><div className="mt-9 inline-block" style={{ background: signal, color: ink }}>{action}</div></div><div className="flex flex-wrap justify-between gap-4 text-[9px] font-bold uppercase tracking-[.16em] opacity-55"><span>{props.brief.audience}</span><span>{props.brief.location || props.brief.deliveryMode}</span></div></div>;
  const assessmentPanel = <div className="relative min-h-[520px] overflow-hidden border-l border-current/20 p-8" style={{ background: `color-mix(in srgb, ${paper} 86%, ${signal})` }}><div className="grid h-full grid-rows-[auto_1fr_auto]"><div className="flex justify-between text-[9px] font-bold uppercase tracking-[.18em]"><span>Initial picture</span><span>01—03</span></div><div className="grid place-items-center"><div className="relative aspect-square w-[72%] rounded-full border border-current/30 before:absolute before:inset-[17%] before:rounded-full before:border before:border-current/30 after:absolute after:inset-[34%] after:rounded-full after:border after:border-current/30"><span className="absolute left-1/2 top-[-5%] h-[110%] w-px bg-current/25" /><span className="absolute left-[-5%] top-1/2 h-px w-[110%] bg-current/25" /><i className="absolute left-[63%] top-[32%] h-4 w-4 rounded-full not-italic" style={{ background: signal }} /></div></div><div className="grid grid-cols-3 border-t border-current/25 pt-4 text-[8px] font-bold uppercase tracking-[.12em]"><span>Assess</span><span>Explain</span><span>Plan</span></div></div></div>;
  const imagePanel = <button disabled={!props.editable} onClick={props.onReplaceImage} className="relative min-h-[560px] overflow-hidden text-left" style={{ background: `color-mix(in srgb, ${paper} 82%, ${signal})` }}>{images[0] ? <img src={images[0]} alt="" className="absolute inset-0 h-full w-full object-cover" /> : assessmentPanel}<span className="absolute bottom-6 left-6 bg-white/90 px-4 py-2 text-[9px] font-bold uppercase tracking-[.16em] text-black">Practitioner / assessment</span></button>;
  const module = (id: string, index: number) => { const n = String(index + 1).padStart(2, "0");
    if (id === "services" && byId.services) return <section key={id} data-module="services" className={pad} style={{ background: paper, color: ink }}><div className="flex justify-between border-t border-current/40 pt-3 text-[9px] font-bold uppercase tracking-[.18em]"><span>{byId.services.eyebrow || "Ways we can help"}</span><span>{n}</span></div><div className={cn("mt-14 grid gap-12", compact ? "grid-cols-1" : "grid-cols-[.75fr_1.25fr]")}><h2 className={heading}>{byId.services.title}</h2><div><p className="max-w-2xl text-lg leading-8 opacity-65">{byId.services.body}</p><div className={cn("mt-9 grid border border-current/30", compact ? "grid-cols-1" : "grid-cols-3")}>{services.slice(0, 3).map((item, i) => <article key={item} className="flex min-h-48 flex-col justify-between border-current/30 p-5 md:border-r"><span className="text-[10px] opacity-45">0{i + 1}</span><b className="text-base leading-6">{item}</b></article>)}</div></div></div></section>;
    if (id === "approach" && byId.approach) return <section key={id} data-module="approach" className={pad} style={{ background: ink, color: paper }}><div className="text-[9px] font-bold uppercase tracking-[.18em]" style={{ color: signal }}>{byId.approach.eyebrow || "Your pathway"} / {n}</div><div className={cn("mt-10 grid gap-14", compact ? "grid-cols-1" : "grid-cols-[.8fr_1.2fr]")}><div><h2 className={heading}>{byId.approach.title}</h2><p className="mt-7 max-w-xl text-base leading-8 opacity-60">{byId.approach.body}</p></div><ol className="border-y border-white/25">{steps.map((item, i) => <li key={item} className="grid grid-cols-[58px_1fr] border-b border-white/20 py-6"><span style={{ color: signal }}>0{i + 1}</span><b className="text-lg">{item}</b></li>)}</ol></div></section>;
    if (id === "schedule") return <section key={id} data-module="schedule" className={pad} style={{ background: paper, color: ink }}><span className="text-[9px] font-bold uppercase tracking-[.18em]" style={{ color: signal }}>Appointments / {n}</span><h2 className={cn(heading, "mt-6")}>Available appointment routes.</h2><div className="mt-10 border-y border-current/35">{schedule.map((item, i) => <div key={item} className="grid grid-cols-[58px_1fr] border-b border-current/20 py-5"><span className="opacity-45">0{i + 1}</span><b>{item}</b></div>)}</div></section>;
    if (id === "about" && byId.about) return <section key={id} data-module="about" className={cn("grid", compact ? "grid-cols-1" : "grid-cols-[.85fr_1.15fr]")} style={{ background: `color-mix(in srgb, ${paper} 88%, ${signal})`, color: ink }}>{images[1] ? <img src={images[1]} alt="" className="h-full min-h-[460px] w-full object-cover" /> : <div className="grid min-h-[390px] place-items-center"><div className="grid h-52 w-52 place-items-center rounded-full border border-current/35 text-7xl precision-display">{(props.brief.businessName || "P").charAt(0)}</div></div>}<div className={pad}><span className="text-[9px] font-bold uppercase tracking-[.18em]">{byId.about.eyebrow || "The practice"} / {n}</span><h2 className={cn(heading, "mt-7")}>{byId.about.title}</h2><Editable part="about" props={props} className="mt-8 max-w-2xl text-lg leading-8 opacity-65">{props.copy.about || byId.about.body}</Editable></div></section>;
    if (id === "results" && proof.length) return <section key={id} data-module="results" className={pad} style={{ background: paper, color: ink }}><div className="flex justify-between border-t border-current/40 pt-3 text-[9px] font-bold uppercase tracking-[.18em]"><span>{byId.results?.eyebrow || "Supplied professional evidence"}</span><span>{n}</span></div><h2 className={cn(heading, "mt-12 max-w-5xl")}>{byId.results?.title || "Professional context, clearly stated."}</h2><div className={cn("mt-11 grid gap-px", compact ? "grid-cols-1" : "grid-cols-2")} style={{ background: `color-mix(in srgb, ${ink} 25%, transparent)` }}>{proof.slice(0, 3).map((item, i) => <article key={item} className="min-h-44 p-6" style={{ background: paper }}><span className="text-[10px]" style={{ color: signal }}>0{i + 1}</span><p className="mt-9 text-lg leading-8">{item}</p></article>)}</div></section>;
    if (id === "testimonial" && props.brief.testimonialQuote) return <section key={id} data-module="testimonial" className={pad} style={{ background: signal, color: ink }}><span className="text-[9px] font-bold uppercase tracking-[.18em]">Client words / {n}</span><blockquote className={cn("precision-display mt-10 max-w-6xl font-medium leading-[1.08] tracking-[-.05em]", mobile ? "text-[2.4rem]" : "text-[4.3rem]")}>“{cleanQuote(props.brief.testimonialQuote)}”</blockquote>{props.brief.testimonialName && <cite className="mt-7 block text-[10px] not-italic uppercase tracking-[.16em]">{props.brief.testimonialName}</cite>}</section>;
    if (id === "faq") return <section key={id} data-module="faq" className={pad} style={{ background: paper, color: ink }}><span className="text-[9px] font-bold uppercase tracking-[.18em]" style={{ color: signal }}>Before booking / {n}</span><div className={cn("mt-9 grid gap-12", compact ? "grid-cols-1" : "grid-cols-[.8fr_1.2fr]")}><h2 className={heading}>{byId.faq?.title || "A useful answer before you book."}</h2><details open className="border-y border-current/35 py-6"><summary className="flex cursor-pointer list-none justify-between gap-4 text-lg font-semibold"><span>{props.brief.faqQuestion}</span><span>+</span></summary><p className="mt-5 max-w-xl text-sm leading-7 opacity-65">{props.brief.faqAnswer}</p></details></div></section>;
    if (id === "contact") return <section key={id} data-module="contact" className={cn(pad, "min-h-[500px]")} style={{ background: ink, color: paper }}><span className="text-[9px] font-bold uppercase tracking-[.18em]" style={{ color: signal }}>Next step / {n}</span><h2 className={cn(heading, "mt-8 max-w-5xl")}>{byId.contact?.title || "Start with the right conversation."}</h2><p className="mt-7 max-w-xl text-lg leading-8 opacity-60">{byId.contact?.body || props.brief.offer || props.copy.subheadline}</p><div className="mt-9 inline-block" style={{ background: signal, color: ink }}>{action}</div></section>;
    return null; };
  const hero = heroVariant === "practitioner" && !mobile ? <section data-hero="practitioner" className="grid grid-cols-[1fr_.82fr]" style={{ background: paper, color: ink }}><div>{heroCopy}</div>{imagePanel}</section> : heroVariant === "pathway" ? <section data-hero="pathway" className={cn("grid", compact ? "grid-cols-1" : "grid-cols-[1.15fr_.85fr]")} style={{ background: ink, color: paper }}><div>{heroCopy}</div><div className="grid grid-rows-3 border-l border-white/20">{(steps.length ? steps : ["Listen", "Assess", "Plan"]).slice(0, 3).map((item, i) => <div key={item} className="flex items-end justify-between border-b border-white/20 p-8"><span className="text-[10px]" style={{ color: signal }}>0{i + 1}</span><b className="max-w-[70%] text-right text-xl">{item}</b></div>)}</div></section> : <section data-hero="assessment" className={cn("grid", compact ? "grid-cols-1" : "grid-cols-[1.15fr_.85fr]")} style={{ background: paper, color: ink }}><div>{heroCopy}</div>{assessmentPanel}</section>;
  return <div className="precision-preview overflow-hidden" style={{ ...vars, background: paper, color: ink, fontFamily: 'Inter,"Segoe UI",Arial,sans-serif' }}><style>{`.precision-preview .precision-display{font-family:var(--precision-display)}.precision-preview button{font-family:inherit}`}</style><header className="flex items-center justify-between border-b border-current/20 px-6 py-5 md:px-12"><b className="text-xs uppercase tracking-[.17em]">{props.brief.businessName}</b><span className="text-[9px] uppercase tracking-[.18em] opacity-45">Precision practice</span></header>{hero}{ordered.map(module)}<footer className="flex flex-wrap justify-between gap-5 border-t border-white/20 px-6 py-9 text-[9px] uppercase tracking-[.16em]" style={{ background: ink, color: paper }}><b>{props.brief.businessName}</b><span>{props.brief.location}</span><span>{props.copy.button}</span></footer></div>;
}

export function buildPrecisionPracticeHtml(props: PrecisionPracticeProps, bookingUrl: string, locale: string) {
  const h = escapeHtml; const r = resolvePrecision(props); const { byId, schedule, ordered, images, heroVariant, fit, displayFont, ink, paper, signal, steps, services, proof } = r;
  const action = `<a class="action" href="${h(bookingUrl)}">${h(props.copy.button)} <span>→</span></a>`;
  const heroSize = fit === "compact" ? "clamp(3.5rem,6.7vw,6.5rem)" : fit === "balanced" ? "clamp(4rem,7.4vw,7.3rem)" : "clamp(4.5rem,8vw,8rem)";
  const label = (left: string, right: string) => `<div class="label"><span>${h(left)}</span><span>${h(right)}</span></div>`;
  const image = (url: string) => url ? `<img src="${h(url)}" alt="" width="1600" height="1200">` : "";
  const heroCopy = `<div class="hero-copy">${label(props.brief.mainService, "ASSESSMENT / 01")}<div><h1>${h(props.copy.headline)}</h1><p>${h(props.copy.subheadline)}</p>${action}</div><div class="hero-meta"><span>${h(props.brief.audience)}</span><span>${h(props.brief.location || props.brief.deliveryMode || "")}</span></div></div>`;
  const assessment = `<div class="assessment"><div class="label"><span>INITIAL PICTURE</span><span>01—03</span></div><div class="target"><i></i></div><div class="assessment-steps"><span>ASSESS</span><span>EXPLAIN</span><span>PLAN</span></div></div>`;
  const hero = heroVariant === "practitioner" ? `<section class="hero practitioner" data-hero="practitioner">${heroCopy}<figure>${image(images[0])}<figcaption>PRACTITIONER / ASSESSMENT</figcaption></figure></section>` : heroVariant === "pathway" ? `<section class="hero pathway" data-hero="pathway">${heroCopy}<div class="pathway-steps">${(steps.length ? steps : ["Listen", "Assess", "Plan"]).slice(0, 3).map((item, i) => `<div><span>0${i + 1}</span><b>${h(item)}</b></div>`).join("")}</div></section>` : `<section class="hero assessment-hero" data-hero="assessment">${heroCopy}${assessment}</section>`;
  const moduleHtml = (id: string, index: number) => { const n = String(index + 1).padStart(2, "0");
    if (id === "services" && byId.services) return `<section class="section services" data-module="services">${label(byId.services.eyebrow || "Ways we can help", n)}<div class="split"><h2>${h(byId.services.title)}</h2><div><p>${h(byId.services.body)}</p><div class="cards">${services.slice(0, 3).map((item, i) => `<article><span>0${i + 1}</span><b>${h(item)}</b></article>`).join("")}</div></div></div></section>`;
    if (id === "approach" && byId.approach) return `<section class="section method" data-module="approach"><small>${h(byId.approach.eyebrow || "Your pathway")} / ${n}</small><div class="split"><div><h2>${h(byId.approach.title)}</h2><p>${h(byId.approach.body)}</p></div><ol>${steps.map((item, i) => `<li><span>0${i + 1}</span><b>${h(item)}</b></li>`).join("")}</ol></div></section>`;
    if (id === "schedule") return `<section class="section" data-module="schedule"><small>APPOINTMENTS / ${n}</small><h2>Available appointment routes.</h2><div class="rows">${schedule.map((item, i) => `<div><span>0${i + 1}</span><b>${h(item)}</b></div>`).join("")}</div></section>`;
    if (id === "about" && byId.about) return `<section class="about" data-module="about">${images[1] ? image(images[1]) : `<div class="monogram">${h((props.brief.businessName || "P").charAt(0))}</div>`}<div class="section"><small>${h(byId.about.eyebrow || "THE PRACTICE")} / ${n}</small><h2>${h(byId.about.title)}</h2><p>${h(props.copy.about || byId.about.body)}</p></div></section>`;
    if (id === "results" && proof.length) return `<section class="section evidence" data-module="results">${label(byId.results?.eyebrow || "Supplied professional evidence", n)}<h2>${h(byId.results?.title || "Professional context, clearly stated.")}</h2><div class="proof">${proof.slice(0, 3).map((item, i) => `<article><span>0${i + 1}</span><p>${h(item)}</p></article>`).join("")}</div></section>`;
    if (id === "testimonial" && props.brief.testimonialQuote) return `<section class="section quote" data-module="testimonial"><small>CLIENT WORDS / ${n}</small><blockquote>“${h(cleanQuote(props.brief.testimonialQuote))}”</blockquote>${props.brief.testimonialName ? `<cite>${h(props.brief.testimonialName)}</cite>` : ""}</section>`;
    if (id === "faq") return `<section class="section faq" data-module="faq"><small>BEFORE BOOKING / ${n}</small><div class="split"><h2>${h(byId.faq?.title || "A useful answer before you book.")}</h2><details open><summary>${h(props.brief.faqQuestion)}<span>+</span></summary><p>${h(props.brief.faqAnswer)}</p></details></div></section>`;
    if (id === "contact") return `<section class="section contact" data-module="contact"><small>NEXT STEP / ${n}</small><h2>${h(byId.contact?.title || "Start with the right conversation.")}</h2><p>${h(byId.contact?.body || props.brief.offer || props.copy.subheadline)}</p>${action}</section>`; return ""; };
  const schema = JSON.stringify({ "@context": "https://schema.org", "@type": "ProfessionalService", name: props.brief.businessName, description: props.copy.subheadline, areaServed: props.brief.location || undefined, url: bookingUrl.startsWith("http") ? bookingUrl : undefined }).replace(/</g, "\\u003c");
  return `<!doctype html><html lang="${h(locale)}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${h(props.brief.businessName)} | ${h(props.brief.mainService)}</title><meta name="description" content="${h(props.copy.subheadline)}"><meta name="theme-color" content="${h(ink)}"><script type="application/ld+json">${schema}</script><style>
:root{--ink:${ink};--paper:${paper};--signal:${signal};--display:${displayFont};--gutter:clamp(1.5rem,5vw,4.5rem)}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--paper);color:var(--ink);font-family:Inter,"Segoe UI",Arial,sans-serif}a{color:inherit}a:focus-visible,summary:focus-visible{outline:3px solid var(--signal);outline-offset:4px}h1,h2,p{margin-top:0}h1,h2,blockquote{font-family:var(--display);font-weight:500}header,footer{display:flex;justify-content:space-between;gap:1rem;padding:1.4rem var(--gutter);font-size:.62rem;font-weight:700;letter-spacing:.17em;text-transform:uppercase}header{border-bottom:1px solid #0003}.hero{display:grid;grid-template-columns:1.15fr .85fr;min-height:42rem}.hero-copy{display:flex;min-height:42rem;flex-direction:column;justify-content:space-between;padding:3rem var(--gutter)}.label,.hero-meta{display:flex;justify-content:space-between;gap:1rem;padding-top:.7rem;border-top:1px solid;font-size:.58rem;font-weight:700;letter-spacing:.17em;text-transform:uppercase}.hero-copy .label span:first-child,small{color:var(--signal)}h1{max-width:64rem;margin:3rem 0 1.75rem;font-size:${heroSize};line-height:.9;letter-spacing:-.07em}.hero-copy p{max-width:39rem;font-size:1.1rem;line-height:1.75;opacity:.65}.action{display:inline-flex;gap:2rem;margin-top:1.5rem;padding:1rem 1.3rem;border:1px solid;text-decoration:none;font-size:.65rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;background:var(--signal)}.assessment{display:grid;grid-template-rows:auto 1fr auto;padding:2rem;background:color-mix(in srgb,var(--paper) 86%,var(--signal));border-left:1px solid #0003}.target{position:relative;align-self:center;justify-self:center;width:70%;aspect-ratio:1;border:1px solid #0005;border-radius:50%}.target:before,.target:after{content:"";position:absolute;border:1px solid #0004;border-radius:50%}.target:before{inset:17%}.target:after{inset:34%}.target i{position:absolute;left:63%;top:32%;width:1rem;height:1rem;border-radius:50%;background:var(--signal)}.assessment-steps{display:grid;grid-template-columns:repeat(3,1fr);padding-top:1rem;border-top:1px solid #0004;font-size:.55rem;font-weight:700;letter-spacing:.12em}.practitioner{grid-template-columns:1fr .82fr}.practitioner figure{position:relative;margin:0;min-height:42rem;overflow:hidden}.practitioner img{width:100%;height:100%;object-fit:cover}.practitioner figcaption{position:absolute;bottom:1.5rem;left:1.5rem;padding:.65rem 1rem;background:#ffffffe6;color:#111;font-size:.55rem;font-weight:700;letter-spacing:.15em}.pathway{background:var(--ink);color:var(--paper)}.pathway-steps{display:grid;grid-template-rows:repeat(3,1fr);border-left:1px solid #fff3}.pathway-steps div{display:flex;align-items:end;justify-content:space-between;padding:2rem;border-bottom:1px solid #fff3}.pathway-steps span{color:var(--signal)}.pathway-steps b{max-width:75%;text-align:right}.section{padding:6rem var(--gutter)}small{font-size:.58rem;font-weight:700;letter-spacing:.17em;text-transform:uppercase}.section>.label{margin-bottom:3.5rem}.split{display:grid;grid-template-columns:.75fr 1.25fr;gap:6vw}h2{margin:0;font-size:clamp(3.2rem,6vw,5.5rem);line-height:.98;letter-spacing:-.055em}.section p,.about p{font-size:1.05rem;line-height:1.85;opacity:.66}.cards{display:grid;grid-template-columns:repeat(3,1fr);margin-top:2.25rem;border:1px solid #0005}.cards article{display:flex;min-height:12rem;flex-direction:column;justify-content:space-between;padding:1.3rem;border-right:1px solid #0005}.cards span,.proof span{font-size:.65rem;opacity:.45}.method,.contact{background:var(--ink);color:var(--paper)}.method ol{margin:0;padding:0;border-block:1px solid #fff4;list-style:none}.method li,.rows>div{display:grid;grid-template-columns:3.5rem 1fr;padding:1.25rem 0;border-bottom:1px solid #fff3}.method li span,.rows span{color:var(--signal)}.rows{margin-top:2.5rem;border-block:1px solid #0005}.rows>div{border-color:#0004}.about{display:grid;grid-template-columns:.85fr 1.15fr;background:color-mix(in srgb,var(--paper) 88%,var(--signal))}.about>img,.monogram{width:100%;height:100%;min-height:30rem;object-fit:cover}.monogram{display:grid;place-items:center;font:500 10rem var(--display)}.evidence h2{max-width:65rem}.proof{display:grid;grid-template-columns:repeat(2,1fr);gap:1px;margin-top:3rem;background:#0004}.proof article{min-height:11rem;padding:1.5rem;background:var(--paper)}.proof p{margin-top:2.5rem;opacity:1}.quote{background:var(--signal)}blockquote{max-width:70rem;margin:2rem 0 0;font-size:clamp(2.7rem,5.5vw,5rem);line-height:1.08;letter-spacing:-.05em}.quote cite{display:block;margin-top:1.5rem;font-size:.65rem;font-style:normal;letter-spacing:.14em;text-transform:uppercase}.faq details{padding:1.5rem 0;border-block:1px solid #0005}.faq summary{display:flex;justify-content:space-between;gap:1rem;font-weight:600;cursor:pointer;list-style:none}.faq details p{margin-top:1.25rem}.contact{min-height:31rem}.contact h2{max-width:66rem;margin-top:2rem}.contact>p{max-width:40rem;color:#fff;opacity:.6}footer{border-top:1px solid #fff3;background:var(--ink);color:var(--paper)}
@media(max-width:760px){.hero,.practitioner,.split,.about{grid-template-columns:1fr}.hero-copy{min-height:38rem;padding:2.5rem 1.5rem}.assessment,.practitioner figure{min-height:26rem}.pathway-steps{border-left:0}h1{font-size:${fit === "compact" ? "2.8rem" : "3.6rem"}}.section{padding:4rem 1.5rem}h2{font-size:2.7rem}.cards,.proof{grid-template-columns:1fr}.cards article{border-right:0;border-bottom:1px solid #0005}.hero-meta{flex-direction:column}}
</style></head><body><header><b>${h(props.brief.businessName)}</b><span>Precision practice</span></header><main>${hero}${ordered.map(moduleHtml).join("")}</main><footer><b>${h(props.brief.businessName)}</b><span>${h(props.brief.location)}</span><span>${h(props.copy.button)}</span></footer></body></html>`;
}
